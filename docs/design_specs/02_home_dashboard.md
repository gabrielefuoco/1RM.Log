# Page Specification: Home Dashboard

**Mockup Source**: [user_home_dashboard_1](../../mockup/user_home_dashboard_1/code.html)

## Overview
Central Hub.
> [!NOTE]
> Configured to match the Global Design System options.

## UI Components

### 1. Hero Card
- **Background**: Dark Surface (`bg-slate-900` or `#162a1e`).
- **Glow**: Top-right corner `bg-primary/20` blur.
- **Button**: `bg-primary text-background-dark`.
- **Icons**: `text-primary`.

### 2. Progress Tracker
- **Completed Days**: `bg-primary text-background-dark`.
- **Shadows**: `shadow-primary/30` (Neon Glow).

### 3. Stats Grid
- **Trend Up**: `text-primary` (Green Arrow).
- **Icons**: `text-primary`.

### 4. Bottom Nav
- **Active Item**: `text-primary`.
- **FAB**: `bg-primary text-background-dark`.
- **Background**: Glass (`backdrop-blur-xl bg-background-dark/80`).
