# Page Specification: Active Workout Session

**Mockup Source**: [active_workout_session_dashboard](../../mockup/active_workout_session_dashboard/code.html)

## Overview
Execution Screen.
> [!NOTE]
> High contrast using `#13ec6d` for all interactive elements and active states.

## UI Components

### 1. Progress Bar
- **Fill**: `bg-primary`.
- **Track**: `bg-white/10`.

### 2. Current Exercise
- **Labels**: `text-primary` ("Current Exercise", "Muscle Group").
- **Best Lift Badge**: `bg-primary text-background-dark`.

### 3. Inputs
- **Active Border**: `focus:border-primary`.
- **Suggestion Text**: `text-primary`.

### 4. Actions
- **Complete Button**: `bg-primary text-background-dark`.
- **Timer**: `text-primary` (when active).

### 5. Up Next
- **1RM Highlight**: `text-primary`.
